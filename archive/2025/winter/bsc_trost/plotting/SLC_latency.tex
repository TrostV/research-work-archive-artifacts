\documentclass[tikz]{standalone}
\usepackage{pgfplots}
\usepackage{pgfplotstable}
\pgfplotsset{compat=1.18}

\input{colors}
\begin{document}

\pgfplotstableread[col sep=semicolon]{data/SLC/AvrgLatency_ST.csv}\mytable
\pgfplotstablegetcolumn{Benchmark}\of\mytable\to\listnames
\pgfplotstablegetrowsof{\mytable}
\pgfmathtruncatemacro{\rowmax}{\pgfplotsretval-1}

\begin{tikzpicture}
    %[trim axis left, trim axis right]
\begin{axis}[
    ybar=0.01cm,
    width=14cm,
    height=7cm,
    ylabel={Average Latency (Cycles)},
    xtick={0,...,\rowmax},
    xticklabel={\pgfmathparse{int(\ticknum)}\pgfplotslistselect\pgfmathresult\of\listnames\to\temp \temp},
    xticklabel style={rotate=45, anchor=north east},
    legend entries={Orig ST, Ours ST, Orig LD, Ours LD},
    legend style={at={(0.5,-0.4)},anchor=north,legend columns=-1},
    ymin = 0,
    table/col sep=semicolon,
    unbounded coords = discard,
    ]
    \addplot[ ybar, fill=orig, draw=none ]
        table [x expr=\coordindex, y expr={(\coordindex < 2 ) ? nan : \thisrow{AvrgLatency}}]
        {data/SLC/AvrgLatency_STorig.csv};
    \addplot[ ybar, fill=cxl, draw=none]
        table [x expr=\coordindex, y expr={(\coordindex < 2 ) ? nan : \thisrow{AvrgLatency}}]
        {data/SLC/AvrgLatency_ST.csv};
       

    \addplot[ ybar, fill=orig_2, draw=none ]
        table [x expr=\coordindex, y expr={(\coordindex > 2 && \coordindex !=5) ? nan : \thisrow{AvrgLatency}}]
        {data/SLC/AvrgLatency_LDorig.csv};
    \addplot[ ybar, fill=cxl_2, draw=none ]
        table [x expr=\coordindex, y expr={(\coordindex > 2 && \coordindex !=5) ? nan : \thisrow{AvrgLatency}}]
        {data/SLC/AvrgLatency_LD.csv};
   
\end{axis}
\end{tikzpicture}
\end{document}